import streamlit as st

i = r"i"
sqrt = r"\sqrt{-1}"
ipm = r"\pm i"
st.title(f"${i}$")
st.write(f"${i}$ is ${sqrt}$. It is an extension to the Real Numbers (ℝ), making it a Complex Number (ℂ). So the answer to ${sqrt}$ is ${ipm}$.")
